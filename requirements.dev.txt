# Dev dependencies — local development and CI only
# Never install in production Docker images
-r requirements.worker.txt

# ── Testing ───────────────────────────────────────────────────────────
pytest>=9.0.2
pytest-asyncio>=1.3.0
pytest-mock>=3.14.0
pytest-cov>=5.0.0
anyio>=4.12.1

# ── Code quality ──────────────────────────────────────────────────────
ruff>=0.4.0
mypy>=1.10.0
