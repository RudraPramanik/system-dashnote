# Operator-only extra for evals/run_ragas.py (laptop / nightly).
# Do NOT add these to requirements/base.txt or the API Docker image.
# ragas>=0.4 provides llm_factory(..., provider="google", client=...).
# Do NOT reintroduce datasets<4 with ragas<0.4 — that forced OpenAI-only 0.3.2.
ragas>=0.4,<0.5
google-genai>=1.0.0
instructor[google-genai]>=1.0.0
datasets>=2.14
httpx>=0.27
python-dotenv>=1.0
