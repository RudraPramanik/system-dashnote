# API dependencies — installed in Dockerfile.api
# DO NOT add: torch, transformers, sentence-transformers, pypdf, python-docx, unstructured

# ── Web framework ─────────────────────────────────────────────────────
fastapi>=0.129.0
uvicorn[standard]>=0.41.0
python-multipart>=0.0.22

# ── Database ──────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.46
asyncpg>=0.31.0
alembic>=1.18.4
psycopg2-binary>=2.9.11

# ── Auth ──────────────────────────────────────────────────────────────
python-jose[cryptography]>=3.5.0
passlib[bcrypt]>=1.7.4

# ── Redis + job queue ─────────────────────────────────────────────────
redis[hiredis]>=7.4.0
arq>=0.26.0

# ── Config + validation ───────────────────────────────────────────────
pydantic>=2.12.5
pydantic-settings>=2.13.0
python-dotenv>=1.2.1
email-validator>=2.3.0
pydantic-extra-types>=2.11.0

# ── AI orchestration (lightweight — no local models) ──────────────────
openai>=1.35.0
langchain-core>=1.4.0
langgraph>=1.2.0
langsmith>=0.1.77
qdrant-client[async]>=1.9.1
tiktoken>=0.7.0

# ── Resilience ────────────────────────────────────────────────────────
tenacity>=8.3.0

# ── Async file I/O ────────────────────────────────────────────────────
aiofiles>=23.2.0
aioboto3>=15.5.0
boto3>=1.40.61

# ── HTTP client ───────────────────────────────────────────────────────
httpx>=0.28.1

# ── Structured logging ────────────────────────────────────────────────
structlog>=24.2.0
sentry-sdk>=2.53.0

# ── MIME detection (metadata only — no file parsing) ─────────────────
python-magic>=0.4.27
