### Deep health (PostgreSQL SELECT 1 + Redis PING when configured)
### 200: status "ok"; 503: status "unavailable" — body always includes timestamp, latency_ms, dependencies
GET http://localhost:8000/health

### Auth
POST http://localhost:8000/auth/register
